# team for project 

To start the web-app: 

cd client/tmaker -> npm start (interface)
cd db -> npm run dev (database). All information is contained in the file tmaker_models.js
cd neuron -> run fastAPI.py (neural network) 